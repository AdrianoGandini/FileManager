/**
 * Module descriptor for the File Organizer application.
 *
 * Defines the module name and its dependencies. The application
 * relies on the {@code java.desktop} module for its Swing-based UI.
 */
module FileOrganizer {
    requires java.desktop;
}
